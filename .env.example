# 매크로 실행 시 브라우저가 열리는 초기 URL (생략 시 about:blank)
START_URL=

# 영상 시청 완료 후 출석(종료) 버튼 클릭 전 안전 버퍼(ms)
END_BUTTON_DELAY_MS=3000

# 플레이어 페이지 로드(=#close_ 버튼 노출) 대기 타임아웃(ms)
PLAYER_READY_TIMEOUT_MS=30000

# duration 만큼 대기한 뒤 추가로 더 기다릴 시간(초). 인트로/로딩 여유.
EXTRA_WAIT_SECONDS=60
